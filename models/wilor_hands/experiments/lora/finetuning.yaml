defaults:
  # Winner-locked full-dataset finetuning baseline for the LoRA suite.
  # Keep this file aligned with the latest checked-in stage winners only.
  # Do not add smoke tests or ad hoc manual variants here.
  # Future edits should preserve the shared long-run full-dataset schedule
  # across suites unless the comparison protocol itself changes.
  train_mode: distill
  train_scope: refine_net
  all_videos: true
  validation_split: 0.15
  sample_limit: 0
  detection_conf: 0.3
  rescale_factor: 2.0
  batch_size: 8
  num_workers: 2
  max_steps: 10000
  log_every: 25
  save_every: 250
  seed: 42
  optimizer:
    lr: 3.0e-5
    weight_decay: 0.0
  temporal:
    window_size: 3
    window_stride: 2
    max_frame_gap: 1
    reduction: smooth_l1
    scorer_hidden_dim: 64
    scorer_layers: 2
    scorer_dropout: 0.0
  lora:
    enabled: true
    rank: 8
    alpha: 16.0
    dropout: 0.0
    block_start: 24
    block_end: 32
    target_modules:
      - qkv
  losses:
    vipe_camera:
      enabled: true
      weight: 0.005
    temporal_camera:
      enabled: true
      formulation: learnable
      weight: 0.1
      scorer_weight: 0.001
    temporal_bbox_projected:
      enabled: true
      formulation: learnable
      weight: 0.1
      scorer_weight: 0.001
    temporal_vipe_camera:
      enabled: true
      formulation: learnable
      weight: 0.01
      smoothness_weight: 0.005
      anchor_weight: 0.001

experiments:
  - name: lora_finetuning
    run_name_suffix: lora_finetuning
