defaults:
  train_mode: distill
  train_scope: refine_net
  videos:
    - clip_2
  validation_split: 0.15
  sample_limit: 0
  detection_conf: 0.3
  rescale_factor: 2.0
  batch_size: 8
  num_workers: 4
  max_steps: 10000
  log_every: 25
  save_every: 250
  seed: 42
  optimizer:
    lr: 1.0e-5
    weight_decay: 1.0e-4
  temporal:
    window_size: 8
    window_stride: 4
    max_frame_gap: 1
    reduction: smooth_l1
    scorer_hidden_dim: 64
    scorer_layers: 2
    scorer_dropout: 0.0
  losses:
    vipe_camera:
      enabled: true
      weight: 0.01
    temporal_camera:
      enabled: false
      formulation: static
      weight: 0.0
      scorer_weight: 0.0
    temporal_bbox_projected:
      enabled: false
      formulation: static
      weight: 0.0
      scorer_weight: 0.0
    temporal_bbox_input:
      enabled: false
      formulation: static
      weight: 0.0
      scorer_weight: 0.0

experiments:
  - name: baseline_vipe_only
    run_name_suffix: baseline

  - name: temporal_camera_only
    run_name_suffix: temp_cam
    losses:
      temporal_camera:
        enabled: true
        formulation: learnable
        weight: 0.01
        scorer_weight: 0.001

  - name: temporal_bbox_projected_only
    run_name_suffix: temp_bbox_projected
    losses:
      temporal_bbox_projected:
        enabled: true
        formulation: learnable
        weight: 0.01
        scorer_weight: 0.001

  - name: temporal_bbox_input_only
    run_name_suffix: temp_bbox_input
    losses:
      temporal_bbox_input:
        enabled: true
        formulation: learnable
        weight: 0.01
        scorer_weight: 0.001

  - name: temporal_all_losses
    run_name_suffix: temp_all
    losses:
      temporal_camera:
        enabled: true
        formulation: learnable
        weight: 0.01
        scorer_weight: 0.001
      temporal_bbox_projected:
        enabled: true
        formulation: learnable
        weight: 0.01
        scorer_weight: 0.001
      temporal_bbox_input:
        enabled: true
        formulation: learnable
        weight: 0.01
        scorer_weight: 0.001

  - name: test_temporal_all_losses
    run_name_suffix: test_temp_all
    train_mode: test
    max_steps: 100
    log_every: 10
    save_every: 50
    sample_limit: 64
    losses:
      temporal_camera:
        enabled: true
        formulation: learnable
        weight: 0.01
        scorer_weight: 0.001
      temporal_bbox_projected:
        enabled: true
        formulation: learnable
        weight: 0.01
        scorer_weight: 0.001
      temporal_bbox_input:
        enabled: true
        formulation: learnable
        weight: 0.01
        scorer_weight: 0.001
